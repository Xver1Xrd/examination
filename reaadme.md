# Install me

## pip

``` bash
pip install pyyaml --user
pip install tkVideoPlayer --user


python3 -m venv .env
source .env/bin/activate
python -m pip install PyQt6
pip install --upgrade pip
python form.py 
python -m pip install pyyaml
python form.py 
python -m pip freeze > requirements.txt
```

